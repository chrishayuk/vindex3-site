import { Hero } from "@chrishayuk/house/components/forms/Hero";

export default function Home() {
	return (
		<main>
			<Hero kicker="VINDEX3 / SPEC" title="A MODEL IS NOT A FILE." dek="A format for executable model representations." />
		</main>
	);
}
